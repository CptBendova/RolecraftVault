# never commit the signing key — it is the root of trust for every update
keys/*.pem

node_modules/
dist/
*.exe
*.rcvup
*.zip

.DS_Store
Thumbs.db

# editor / local dev tooling
.claude/
keys/*.jks
keys/*.keystore
keys/*.p12
keys/android-keystore.txt
mobile/android/keystore.properties
tmp-vault/
